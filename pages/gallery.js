import React from "react";
import styles from "../styles/gallery.module.scss";

const Gallery = () => {
	return (
		<div className={styles.galleryContainer}>
			An image gallery, reviews or an online ordering page could go here...
		</div>
	);
};

export default Gallery;
